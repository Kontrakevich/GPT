# MARINS greetings mobile mirror

This public page reads `snapshot.json`, which is generated by the private `marins-reminder-bot` runtime. The mirror is read-only and contains only hotel name, greeting text, start/end dates, and placement status.
